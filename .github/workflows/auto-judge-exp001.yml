name: DEPRECATED - EXP-001 GitHub Models judge

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Models retired
        run: |
          echo 'This workflow is intentionally disabled.'
          echo 'GitHub Models inference was retired on 2026-07-30.'
          echo 'Use the Copilot CLI EXP-001 judging workflow instead.'
          exit 1
