# ThresholdSpec research

This branch contains a reproducible CPU experiment for speculative-decoding inference. It measures whether approximate low-precision evaluations preserve the accept/reject decision of a full-precision pretrained language-model verifier. It does not modify NOVUM 2.0.0 and does not claim a production speedup or a formal correctness proof.
